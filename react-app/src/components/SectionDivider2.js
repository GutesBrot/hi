import React from 'react';
import '../App.css'; // Ensure this path is correct to apply global styles

const SectionDivider2 = () => {
    return (
        <div className="spacer layer1 flip"></div>
    );
};

export default SectionDivider2;
